// Hằng số dùng chung toàn app.

export const ROLES = {
  USER: 'user',
  ADMIN: 'admin',
};

// TODO: bổ sung danh mục vùng miền / tôn giáo / loại ngày lễ dùng cho bộ lọc
export const REGION_OPTIONS = [];
export const RELIGION_OPTIONS = [];
export const EVENT_CATEGORY_OPTIONS = [];
