import api from './api.js';

// Đề xuất điểm bán / địa điểm thờ cúng + tích hợp bản đồ (GPS, chỉ đường)
// TODO: implement các hàm gọi API tương ứng.

export default {
  // getAll: () => api.get('/...'),
  // getById: (id) => api.get(`/.../${id}`),
  // create: (data) => api.post('/...', data),
  // update: (id, data) => api.put(`/.../${id}`, data),
  // remove: (id) => api.delete(`/.../${id}`),
};
